<table class="royal-table" ...attributes>
  {{#if @caption}}
    <caption>
      {{@caption}}
    </caption>
  {{/if}}
  <thead>
    {{yield
      (component
        'royal-table/row'
        place='head'
        onClickHead=@onClickHead
        onSort=@onSort
        sortingProp=@sortingProp
        sortingIconComponent=@sortingIconComponent
      )
    }}
  </thead>
  <tbody>
    {{#each @data as |item index|}}
      {{yield (component 'royal-table/row' place='body' data=item) item index}}
    {{/each}}
  </tbody>
</table>
