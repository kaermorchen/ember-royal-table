<table class="royal-table" ...attributes>
  <thead>
    <tr>
      {{yield (component "royal-column" place="head" onClickHead=@onClickHead onSort=@onSort sortingProp=@sortingProp sortingIconComponent=@sortingIconComponent)}}
    </tr>
  </thead>
  <tbody>
    {{#each @data as |item index|}}
      <tr>
        {{yield (component "royal-column" place="body" data=item) item index}}
      </tr>
    {{/each}}
  </tbody>
</table>
